# core (torch/torchvision come from the CUDA base image, see docker/Dockerfile)
diffusers>=0.38.0
transformers>=4.49.0
accelerate>=1.4.0
peft>=0.19.0
bitsandbytes>=0.43.3
sentencepiece>=0.2.0
protobuf>=4.25
safetensors>=0.4.5
einops>=0.8.0
huggingface_hub
datasets>=3.0.0
pyarrow
pillow
wandb
tqdm
cairosvg          # SVG -> PNG rendering (needs system libcairo2)
# metrics (OmniSVG parity): install these two with --no-deps, see Dockerfile
scipy
ftfy
regex
clint
timm>=0.9
# openai-clip  (--no-deps)
# hpsv2        (--no-deps, then copy bpe_simple_vocab_16e6.txt.gz from clip/ into hpsv2/src/open_clip/)
